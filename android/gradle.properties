org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template.
# Keep it false until flutter_local_notifications supports built-in Kotlin;
# this also means file_picker must stay on 10.x (11.x skips applying the
# Kotlin Gradle plugin on AGP 9+ and fails to compile with this off).
android.builtInKotlin=false
# Workaround for Kotlin incremental-cache file locking on Windows
# ("Could not close incremental caches" in plugin compileDebugKotlin tasks).
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process
